[package]
name = "rescind-sim"
description = "rescind's shadow world: seeded events against the engine, the invariants of docs/ROADMAP.md 10.3 after every one, and a shrinker over the events that broke one"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rescind-core = { path = "../core" }
rescind-engine = { path = "../engine" }
rescind-render = { path = "../render" }
serde_json.workspace = true
