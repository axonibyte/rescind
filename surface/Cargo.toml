[package]
name = "rescind-surface"
description = "The .scind front end: lexer, parser with recovery over a lossless tree, formatter, and (from unit B) the resolver to the plan IR"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rescind-core = { path = "../core" }
logos.workspace = true
rowan.workspace = true
serde.workspace = true
toml.workspace = true
