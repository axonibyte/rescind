[package]
name = "rescind-render"
description = "Backstop artifact rendering per OS family and artifact language, with per-family quoting; pure, depends only on rescind-core"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rescind-core = { path = "../core" }

[dev-dependencies]
# The execution tests write completion markers with real digests.
sha2.workspace = true
